services:
  - type: web
    name: ai-memory-backend
    env: python
    rootDir: ai_memory_system
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /
    autoDeploy: true
